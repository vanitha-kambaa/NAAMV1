// Firebase configuration helper
// This reads Firebase project info from google-services.json and GoogleService-Info.plist

export const FIREBASE_CONFIG = {
  projectId: 'namm-105d6',
  projectNumber: '74998480991',
  // Note: This is Firebase project ID, NOT Expo projectId
  // Expo projectId must be a UUID format
};
